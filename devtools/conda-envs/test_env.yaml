name: test
channels:
 - conda-forge
 - defaults
dependencies:
   # Base depends
 - python
 - pip

 # Package dependencies
 - numpy
 - matplotlib
 - random
 - networkx

 # Testing
 - pytest
 - pytest-cov
 - codecov

  # Pip-only installs
 #- pip:
 #  - codecov